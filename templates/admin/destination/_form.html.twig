{{ form_start(form) }}
    {{ form_row(form.name) }}
     {{ form_row(form.description) }}
      {{ form_row(form.duration) }}
       {{ form_row(form.price) }}
        {{ form_row(form.type) }}
       <img src="{{ asset('/uploads/'~ destination.image) }}" style="width: 120px;height: 120px;display: block;"/>
        {{ form_row(form.image) }}
    <button class="btn btn-success float-end">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
